# Keep remote CLI key revocation out of the CLI

BeeCrawl CLI will not provide a remote key revocation command or a `--revoke` logout mode. It only removes local profile data; users manage the dedicated remote key from Dashboard, keeping CLI logout simple, offline-safe, and free of another browser authorization flow.

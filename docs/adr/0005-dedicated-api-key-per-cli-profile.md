# Create a dedicated API key for each CLI profile

Dashboard authorization will create a separate BeeCrawl API key for the selected CLI profile instead of exposing or reusing an existing user key. The key can therefore be revoked independently, while the Dashboard remains responsible for workspace membership and key creation authorization.

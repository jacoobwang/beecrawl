# Expose common search filters as first-class flags

The first CLI release will expose search limit, source, category, included and excluded domains, language, and country as repeatable or scalar flags. Less common v2 search fields such as location, time filters, and nested scrape options remain available through the JSON options escape hatch.

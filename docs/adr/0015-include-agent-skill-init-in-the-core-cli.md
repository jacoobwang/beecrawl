# Include core Agent Skill installation in the first CLI release

`beecrawl init` will detect supported Coding Agents and install a BeeCrawl Agent Skill covering the first-release command set. The initial skill will not advertise browser or interaction commands until those CLI capabilities are shipped, keeping onboarding instructions aligned with the executable surface.
